name: Docker - AutoClient


on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
    paths:
      - 'Tools/AutoClient/**'
      - '!Tools/AutoClient/docker/docker-compose.yaml'
      - 'Framework/**'
      - 'ProjectPlugins/**'
      - .github/workflows/docker-autoclient.yml
  workflow_dispatch:


jobs:
  build-and-push:
    name: Build and Push
    uses: promethei-project/github-actions/.github/workflows/docker-reusable.yml@main
    with:
      docker_file: Tools/AutoClient/docker/Dockerfile
      dockerhub_repo: durabilitylabs/promethei-autoclient
      tag_latest: ${{ github.ref_name == github.event.repository.default_branch || startsWith(github.ref, 'refs/tags/') }}
    secrets: inherit
